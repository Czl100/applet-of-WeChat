<view class='c'>
<block wx:for="{{postList}}" wx:for-item="item" wx:for-index="idx" wx:key="1">

<view wx:if="{{item.finish}}">  
<view class='container-a'>
<image wx:if="{{!finish}}" mode='aspectFit' catchtap='onpre'  src="{{item.img}}"></image>
<image wx:if="{{finish}}" mode='aspectFit' catchtap='onpre' src="{{img_unfinish}}"></image>
<text>{{item.imgtitle}}</text>
<text wx:if="{{!finish}}">{{item.datetime}}</text>
<text wx:if="{{finish}}">未完成</text>
</view>
</view>
</block>
</view>

<view class='a'>
<view class='container-b'>
 <button class="weui-btn mini-btn" type="primary" size="mini" catchtap='onbefore'>上一页</button>
            <button class="weui-btn mini-btn" type="default" size="mini">{{mypage}}</button>
            <button class="weui-btn mini-btn" type="warn" size="mini" catchtap='onafter'>下一页</button>
</view>
</view>